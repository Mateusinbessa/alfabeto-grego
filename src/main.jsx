import ReactDOM from 'react-dom/client'
import { Alfabeto } from './components'
import 'src/styles/index.css'
import React from 'react'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Alfabeto />
  </React.StrictMode>,
)
